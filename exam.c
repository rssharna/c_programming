#include<stdio.h>
//write  a c program to find the integer between a range that are divisble by 3 or 5 but not both using either continue or break.
void prod(int x, int y)
{
    printf("%d\n",x*y);
}
int main()
{
    prod(2,3);

    return 0;
}
